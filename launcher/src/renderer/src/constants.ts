// Kuratierte Akzentfarben aus dem Redesign — Tags ohne eigene Farbe rotieren
// automatisch nach Position durch diese Liste (siehe Sidebar.tsx), eine
// explizit gewählte Farbe (siehe cycleTagColor) übersteuert das nur für den
// einzelnen Tag.
export const TAG_COLORS = ['bg-gold', 'bg-ember', 'bg-rust', 'bg-amber']
